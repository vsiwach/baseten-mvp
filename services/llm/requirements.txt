# Runtime deps for the LLM serving image. The simulator itself is stdlib-only;
# fastapi/uvicorn serve it, httpx is used only by the container/sklearn adapters.
fastapi==0.115.12
uvicorn==0.34.2
httpx==0.28.1
pydantic==2.11.4
